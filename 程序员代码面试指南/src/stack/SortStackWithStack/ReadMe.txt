用一个栈实现另一个栈的排序
一个栈中元素的类型为整型，现在要将该栈才哦你个顶到底从大到小的顺序排序，只允许申请一个栈。
除此之外，可以换申请新的变量，但不能申请额外的数据结构。

-------------------------------------
我的思路：
首先正常push到一个栈中，然后调用一个sort方法，再进行排序并填充到该栈中。
如何排序？
难以实现。
-------------------------------------
书的思路：和我的一致。
如何排序？
一、遍历数据栈
0、数据栈弹出一个元素
1、while(如果help辅助栈不为空 && help辅助栈的顶部 < stack数据栈的顶部元素)
   {
     弹出辅助栈顶部元素 --> 进stack数据栈
   }
2、如果为空
   或者
   help辅助栈的顶部 > stack数据栈的顶部元素：则弹出的元素直接添加进辅助栈


二、遍历辅助栈
0、把辅助栈的所有元素push到数据栈

----------------------------------------

